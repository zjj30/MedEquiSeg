\begin{table}[!htbp]
\centering
\caption{Reproducibility and implementation checks for the reported experiments.}
\label{tab:protocol_implementation_audit}
\scriptsize
\setlength{\tabcolsep}{3.0pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.19\linewidth}>{\raggedright\arraybackslash}p{0.46\linewidth}>{\raggedright\arraybackslash}p{0.27\linewidth}@{}}
\toprule
Verification item & Evidence & Interpretation \\
\midrule
Data and evaluation consistency & Dataset partitions, split roles, mask
processing, random seeds, prediction threshold, and evaluation metrics were
constant across comparisons; the training policy is documented separately. &
Supports comparisons under common data and evaluation procedures, but not a
claim that all training choices were fixed prospectively. \\
\addlinespace[2pt]
Shared augmentation policy & The initial policy restricted ClinicDB and COVID-19
to horizontal flipping and excluded continuous geometric transformations. The
shared policy applies multiple geometric transformations plus mild affine,
translation, and zoom operations to all datasets. & Introduced after the initial
augmentation specification; treated as a retrospective follow-up, not a
prospectively specified factorial component. \\
\addlinespace[2pt]
Active ATConv path & The model contains four registered replacements. Module-activation
monitoring on a real BUSI test case recorded one non-zero output for each projector
replacement and no activation of the two neck modules. & The effective forward
pass contains two active projector ATConv operators. \\
\addlinespace[2pt]
Experiment traceability & Records link the data partitions, source files, text
cache, training settings, epoch, and selected model. The summary omits batch size
and worker count, but supplementary execution records retain them. Identity checks
for all 75 retained models agreed with the archived records. & Results remain
model-traceable; the omitted settings are disclosed as a reproducibility limitation. \\
\addlinespace[2pt]
Source reconstruction & The archived source package omits four orchestration and
evaluation files, and the available release copies cannot be verified as identical
to the source used for the aggregate experiments. & Exact reconstruction of the
original software environment is not guaranteed. \\
\addlinespace[2pt]
Training-log completeness & Seventy-four of 75 retained logs contain 100 epoch records with
consistent model-selection records. One intermediate BioMedCLIP--ATConv
COVID-19 seed-789 log was overwritten during an unintended repeated execution; its
metadata and four subsequent verification analyses identify the same selected
model. & The numerical result remains model-traceable, but the associated
training log is incomplete. \\
\bottomrule
\end{tabular}
\end{table}
